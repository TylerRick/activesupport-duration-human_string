RSpec.describe Activesupport::Duration::HumanString do
  it "has a version number" do
    expect(Activesupport::Duration::HumanString::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
